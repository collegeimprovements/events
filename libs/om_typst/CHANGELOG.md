# Changelog

## 0.1.0

- Initial release
- Compile `.typ` files to PDF, PNG, SVG, or HTML
- Stream-based compilation via ExCmd
- String content compilation
- Watch mode with callbacks
- Document querying with selector syntax
- Font listing and management
- Project initialization from templates
